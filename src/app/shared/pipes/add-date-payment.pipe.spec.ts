import { AddDatePaymentPipe } from './add-date-payment.pipe';

describe('AddDatePaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new AddDatePaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
